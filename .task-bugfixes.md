Fix ALL 7 bugs found in the code review:

1. HIGH - Invader flash timer never advanced
   File: src/game/Invader.js + src/game/InvaderGrid.js
   Fix: Add an update(dt) method to Invader that decrements flashTimer and restores original material color. Call invader.update(dt) from InvaderGrid.update().

2. HIGH - Duplicate Background and PostFX instances
   Files: src/graphics/Renderer.js + src/main.js
   Fix: Remove Background and PostFX creation from Renderer.js. Keep the ones created in main.js and passed to Game.js. Renderer should only manage the WebGL/WebGPU renderer, scene, and camera.

3. MEDIUM - Player bullets survive wave transitions
   File: src/game/Game.js
   Fix: In startNextWave(), also clear playerBullets array and remove their meshes from the scene.

4. MEDIUM - renderer.update() never called (dead code)
   File: src/game/Game.js + src/graphics/Renderer.js
   Fix: Call renderer.update(dt) from the game loop, OR move the shake/animation logic into Game.js. Make screen shake actually work.

5. MEDIUM - Star sizes not rendered (PointsMaterial ignores per-vertex size)
   File: src/graphics/Background.js
   Fix: Use THREE.ShaderMaterial with gl_PointSize set from the size attribute, or use sizeAttenuation with individual Points objects for each layer at different sizes.

6. LOW - Unused _vec3 variable
   File: src/graphics/ParticleSystem.js line 4
   Fix: Remove it.

7. LOW - Leaked BoxGeometry in Barrier.js
   File: src/game/Barrier.js
   Fix: After cloning per block, dispose the template BoxGeometry.

After fixing all bugs, run: npm run build
