Final polish pass for Cosmic Invaders. Working directory: /home/carlos/Projects/cosmic-invaders/

After codex-1 does integration, you do the polish pass. Read all src/ files first.

1. Add visual juice:
   - ParticleSystem explosions on invader kills (call from Game.js when collision detected)
   - Bullet trail particles
   - Screen shake on player hit (camera offset that decays)
   - Flash effect on invader hit (not killed)
   - Smooth wave transition (brief pause, "WAVE N" text, then new formation)

2. Tune the difficulty curve:
   - Wave 1: slow invaders, low fire chance
   - Each wave: slightly faster movement, higher fire chance, more tough/elite invaders
   - Speed boost when fewer than 5 invaders remain
   - Boss row (elite) shoots more often

3. Add the PostFX pipeline:
   - Wire PostFX.js into Renderer so bloom and CRT effects are active
   - Make sure the bloom highlights bullets and explosions
   - CRT scanlines should be subtle (opacity ~0.1)

4. Add the Background starfield:
   - Wire Background.js into the scene
   - Stars should scroll slowly downward
   - Parallax layers create depth

5. Use Models.js for all entity meshes:
   - Replace any placeholder geometries in Player.js, Invader.js, Bullet.js with Models.js factory calls
   - Make sure invader types look visually distinct

6. Add Object pooling for bullets (src/utils/Pool.js):
   - Simple pool that pre-allocates N objects, get()/release() pattern
   - Use for player and enemy bullets to avoid GC pressure

Make all changes directly. Run npm run build to verify.
