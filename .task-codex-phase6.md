Integration and bug fixing for Cosmic Invaders. Working directory: /home/carlos/Projects/cosmic-invaders/

The game has all modules created but they are not wired together yet. Your job:

1. Update src/main.js to import and initialize ALL modules:
   - Renderer, Game, Input, Score, InvaderGrid, Player, Background, ParticleSystem, PostFX, AudioManager, HUD, Menu, Models
   - Create proper initialization sequence: Renderer.init() (async), then create game objects, start loop
   - Wire Input to Player movement and shooting
   - Wire collision detection into the game loop
   - Wire Score updates on kills
   - Wire AudioManager sound effects to game events
   - Wire HUD updates each frame
   - Wire Menu for title/gameover/pause screens

2. Update src/game/Game.js to be the central coordinator:
   - MENU state: show Menu title, wait for Enter
   - PLAYING state: update Player, InvaderGrid, Bullets, check Collisions, update Score, update HUD
   - PAUSED state: show pause overlay
   - GAMEOVER state: show game over screen
   - Handle wave completion (all invaders dead -> next wave)
   - Spawn barriers at game start
   - Manage bullet arrays (player bullets + enemy bullets)
   - Remove off-screen bullets
   - Handle power-up spawning and collection

3. Fix any import path issues - make sure all imports resolve correctly.

4. Run npm install and npm run build to verify everything compiles. Fix any errors.

Read the existing files first to understand the APIs: Config.js, Player.js, InvaderGrid.js, Invader.js, Bullet.js, Collision.js, Barrier.js, PowerUp.js, Score.js, Renderer.js, ParticleSystem.js, Background.js, Models.js, AudioManager.js, HUD.js, Menu.js.
